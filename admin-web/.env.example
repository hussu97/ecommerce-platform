# Admin Web – copy to .env. Prefer 127.0.0.1 over localhost. See admin-web/README.md.

# Admin API base URL (include /v1). Unset = /api (Vite proxy in dev).
VITE_API_URL=http://127.0.0.1:8001/v1
# In Docker build: set to admin-api service URL (e.g. http://admin-api:8001/v1) or public URL.
