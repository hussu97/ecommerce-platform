# Customer BFF – copy to .env. Proxies /v1/* to customer-api. See ARCHITECTURE.md.

# Customer API URL (no trailing slash)
CUSTOMER_API_URL=http://127.0.0.1:8000
# In Docker: CUSTOMER_API_URL=http://customer-api:8000

# Rate limit: requests per minute per IP (0 = disabled)
RATE_LIMIT_PER_MIN=100
